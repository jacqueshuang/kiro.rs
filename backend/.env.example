# Kiro.rs 配置文件示例
# 复制此文件为 .env 并修改配置

# 服务器配置
HOST=127.0.0.1
PORT=8080

# API 密钥（必填，用于验证 API 请求）
API_KEY=sk-kiro-rs-your-secret-key

# 默认管理员账号（仅首次初始化时使用）
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=admin123

# 数据库路径（可选，默认 data.db）
# DB_PATH=data.db

# JWT 配置（可选，不配置则自动生成）
# JWT_SECRET=your-jwt-secret-key-at-least-32-characters
# JWT_EXPIRY_HOURS=24

# 注意：以下配置存储在数据库中，通过 Admin 后台管理
# - Kiro Version、System Version、Node Version
# - HTTP 代理配置
# - count_tokens API 配置
